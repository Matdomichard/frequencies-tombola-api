# BDD
spring.datasource.url=jdbc:postgresql://localhost:5432/tombola
spring.datasource.username=tombola_user
spring.datasource.password=secret
spring.security.user.name=admin
spring.security.user.password=motdepasse
spring.security.user.roles=ADMIN


# HelloAsso
helloasso.client-id=${HELLOASSO_CLIENT_ID}
helloasso.client-secret=${HELLOASSO_CLIENT_SECRET}
helloasso.api-url=${HELLOASSO_API_URL:https://api.helloasso.com} # par d�faut prod
helloasso.organization-slug=${HELLOASSO_ORGANIZATION_SLUG}

# Autres options Spring
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Cors allowd origins
app.cors.allowed-origins=http://localhost:4200,https://monapp.vercel.app

